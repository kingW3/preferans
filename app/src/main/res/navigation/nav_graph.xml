<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/gameFragment">

    <fragment
        android:id="@+id/gameFragment"
        android:name="com.example.preferans.GameFragment"
        android:label="Game Fragment"
        tools:layout="@layout/fragment_game" >
        <!-- Define actions for navigation to other fragments if needed -->
    </fragment>

    <fragment
        android:id="@+id/scoreFragment"
        android:name="com.example.preferans.ScoreFragment"
        android:label="Score Fragment"
        tools:layout="@layout/fragment_score" >
        <!-- Define actions for navigation to other fragments if needed -->
    </fragment>

    <!-- Include other fragments (BiddingFragment, SelectGameFragment) here -->

</navigation>